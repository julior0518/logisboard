/* beautiful-react-hooks version: 0.35.0 */
import useTouchEvents from './useTouchEvents.js';
import useTouchState from './useTouchState.js';

var useTouch = function useTouch() {
  var ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
  var state = useTouchState(ref);
  var events = useTouchEvents(ref);
  return [state, events];
};

export default useTouch;
